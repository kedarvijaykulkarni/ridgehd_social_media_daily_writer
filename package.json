{
  "name": "aquaroster-social-daily",
  "version": "0.1.0",
  "private": true,
  "description": "CLI that turns one AquaRoster marketing knowledge-base topic into reviewable X/LinkedIn/Instagram/Reddit drafts using a local Ollama model.",
  "type": "module",
  "bin": {
    "aquaroster-social-daily": "./src/index.js"
  },
  "scripts": {
    "generate": "node src/index.js generate",
    "generate:dry-run": "node src/index.js generate --dry-run",
    "test": "node --test src/**/__tests__/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "dotenv": "^16.4.5"
  }
}
